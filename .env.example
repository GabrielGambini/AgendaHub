# Para testar com Redis localmente, copie para .env.local e preencha.
# Na Vercel: Project → Storage → Marketplace → Upstash Redis → Add Integration

UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Variáveis legadas (se usar KV antigo da Vercel):
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
